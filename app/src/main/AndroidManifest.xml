<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.tec.wodable">

    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wodable">
        <activity android:name=".EjerciciosActivity"/>
        <activity android:name=".edit_info_personal" />
        <activity android:name=".RaceDetailsActivity" />
        <activity android:name=".ForgotPasswordMichel"/>
        <activity android:name=".UserActivity_Michel" />
        <activity android:name=".SignUpActivity_Michel" />
        <activity android:name=".LoginActivityMichel" />
        <activity android:name=".RaceActivity"/>
        <activity android:name=".DailyStepsActivity" />
        <activity android:name=".RecentActivity" />
        <activity android:name=".ProfileInfoActivity" />

        <activity android:name=".LoadingScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>